# Phase 4 Code Review

## 變更範圍

合併 2064338446452465664（原「一般充值」）與 2072532512957681664（「每日充值回馈」）
為同一顯示名稱，數據合併。

## 修改檔案

| 檔案 | 變更 |
|------|------|
| `Dashboard/aggregate.py` | `KNOWN_ACTIVITY_NAMES` 映射改為每日充值回馈；`_bonus_for()` 新增同顯示名稱合併邏輯 |
| `tests/test_aggregate.py` | 更新既有測試斷言；新增 `test_same_display_name_merged` |
| `docs/STATUS.md` | 階段狀態更新 |

## 建置與測試

- 14 項單元測試全部通過（`uv run pytest -v`）
- lint 無錯誤

## 審查結果

### 正確性 ✅
- 同名活動（每日充值回馈 × 2）數據合併：到帳、觸發、人數、次數皆累加
- 兌現率 / 領取率從合併後總量重新計算，避免各自比率加權平均錯誤
- 不同名活動不受影響（如新人首充 vs 每日充值回馈維持獨立）

### 邊界條件 ✅
- 僅其中一個活動有快照 → 合併後單一條目
- 兩個活動不同日開始 → 各自 delta 分別計算後合併
- 前端 `index.html` 零修改，`renderBonus()` 泛用卡片渲染相容

### 安全性 ✅
- 無外部輸入操作
- 無 SQL / shell injection 風險

## 結論

**APPROVED** — 變更範圍精確，測試覆蓋合併邏輯，無安全問題。
