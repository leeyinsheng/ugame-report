# Phase 6 功能驗證

## 驗證項目

### 1. 活動名稱映射

| 活動 ID | 舊名稱 | 新名稱 |
|---------|--------|--------|
| 2064338446452465664 | 一般充值 | → **每日充值回馈** |
| 2072532512957681664 | 每日充值回馈 | ← 維持不變 |

`_activity_display()`：2064338446452465664 映射正確 ✅

### 2. 合併邏輯

- `_bonus_for()` 中，`merged` dict 以顯示名稱分組
- 到帳彩金、觸發彩金、觸發次數、觸發人數、領取人數 → 全部累加
- 兌現率從合併後總量重新計算（credit / trig）
- 領取率從合併後總量重新計算（領取人數 / 觸發人數）
- 驗證測試 → `test_same_display_name_merged` ✅

### 3. 向前相容

- 不同名活動（新人首充100%豪禮 vs 老虎救援金）維持獨立條目 ✅
- `test_same_name_diff_id_preserved` 繼續通過（2064338 + 2072527 不同名 → 2 條目）✅
- 前端 `index.html` 零修改，`renderBonus()` 泛用卡片渲染機制直接取用後端名稱 ✅

### 4. 增量（delta）計算

- 跨日 delta 在合併後正確計算：
  - `当日新增到帐` = Σ(credit - prev_credit) 每活動
  - `当日新增次数` = Σ(count - prev_count) 每活動
- `test_daily_delta` ✅

## 結論

**VERIFIED** — 所有需求已實作並通過測試。等待用戶驗收部署。
